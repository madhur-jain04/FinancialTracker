// const validTransaction = (req, res, next) => {
//     const {amount, category ,type , data} =req.body;

//     if(amount == null || amount<=0){
//         return res.status(400).json({message:"Invalid amount or amount cannot be less than or equal to zero"});
//     }

//     if(category == null || cateegory.trim() === '' || type == null){
//         return res.status(400).json({message:"Category and type are required"});

//     }
    
//     if()
// }